import styles from '../styles/Logo.module.css';

function Logo() {
  return (
    <div>
      <main className={styles.main}>
        <h1 className={styles.title}>
          LOGO COMPONENT
        </h1>
      </main>
    </div>
  );
}

export default Logo;
